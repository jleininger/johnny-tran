import Car from './car.js';

class TranCar extends Car {
    constructor(x, y, width, height, color) {
        super(x, y, width, height, color);
    }
    
    chase(targetX, targetY) {
            
    }
}

export default TranCar;